STATE 0 {
    STATEMENT ('a', 'b', 'L') -> 1;
    STATEMENT ('a', 'a', 'R') -> 2;
    LOOP {
        STATEMENT ('b', 'a', 'R') -> 0;
        STATEMENT ('a', 'a', 'L') -> 0;
    }
    STATEMENT ('b', 'b', 'L') -> HALT;
}

STATE HALT {
    LOOP {
        STATEMENT ('a', 'a', 'L') -> HALT;
    }
}

STATE 2 INSERT 'a' -> 0